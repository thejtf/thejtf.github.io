<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>{{ config.feed.title or config.title }}</title>
    <link>{{ url }}</link>
    <description>{{ config.description }}</description>
    <language>zh-CN</language>
    <lastBuildDate>{% if posts.first().updated %}{{ posts.first().updated.toDate().toUTCString() }}{% else %}{{ posts.first().date.toDate().toUTCString() }}{% endif %}</lastBuildDate>
    {% for post in posts.toArray() %}
    <item>
      <title>{{ post.title }}</title>
      <link>{{ post.permalink | uriencode }}</link>
      <pubDate>{{ post.date.toDate().toUTCString() }}</pubDate>
      <description><![CDATA[{{ post.content | noControlChars | safe }}]]></description>
    </item>
    {% endfor %}
  </channel>
</rss>